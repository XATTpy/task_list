<%= simple_form_for [@task, Comment.new] do |f| %>

  <div class='form-inputs'>
    <%= f.input :text, label: false %>
    <%= f.input :commenter, input_html: {value: current_user}, readonly: true, as: :hidden %>
  </div>

  <div class='form-actions'>
    <%= f.button :submit %>
  </div>
<% end %>